apiVersion: v1
kind: ConfigMap
metadata:
  name: test-app-config
  labels:
    app.kubernetes.io/name: test-app
    app.kubernetes.io/component: config
data:
  NODE_ENV: "production"
  APP_ENV: "kubernetes-production"
  PORT: "80"
